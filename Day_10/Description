treeInfo:
Variables:
head: Represents the head node of the linked list.
tail: Represents the tail node of the linked list.
Constructor:
Initializes the head and tail of the treeInfo object.
inOrder(Node root):
Input: Takes a Node as the root of the binary tree.
Output: Returns a treeInfo object containing the head and tail nodes of the linked list.
Purpose: Recursively converts the binary tree to a circular doubly linked list using in-order traversal.
