Start with the initial vertex (here, 0) and enqueue it.
While the queue is not empty, dequeue a vertex (curr) and process it if it's not visited.
Mark the current vertex as visited, add it to the result (ans), and enqueue its unvisited neighbors.
